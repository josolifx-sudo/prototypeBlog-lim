# ThreadsBlog Client (Vue)

This is the Vue frontend for the Blog prototype.

## Setup

```bash
npm install
npm run dev
```

## Environment

Uses `VITE_API_BASE_URL` from `.env`.

The client calls API routes under `/api/*`.
